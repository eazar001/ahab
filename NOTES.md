Compilation/save-state
----------------------

```
./swipl_logtalk_qlf.sh -c -x -d "$HOME"/collect -p $LOGTALKHOME/paths/paths.pl -n ahab -s "$SETTINGS/settings.lgt" -l $ROOT/src/loader.lgt -g "::(ahab, write_score_output)"
```